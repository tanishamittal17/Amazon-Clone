# Amazon-Clone
An **Amazon Clone** built using **HTML and CSS** is a static e-commerce website that replicates the basic layout and design of Amazon. It includes a navigation bar with a logo, search box, and cart icon, along with product sections displayed in grid format. HTML is used to structure the webpage, while CSS is used for styling, and layout.
